# YARP dependencies

At the very least, we can shove all our conda environment files into this folder.

For now, I'm just going to treat this as a working list of all the packages I've installed, **and why**.
- `conda install python`: When I ran this, it pulled in version 3.13.2
- `conda install pyaml`: For reading YAML input files from command-line